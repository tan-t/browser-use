version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - PORT=8000
    ports:
      - "8000:8000"
    command: ["python", "/app/browser_use/server.py"]
